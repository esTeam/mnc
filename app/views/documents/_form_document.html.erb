
  <p class=form_note> שדות המסומנים ב * הם שדות חובה </p> 
  
  <p>
     <label><%=astrisk%>שם מזהה (זהה בכל השפות)</label>
     <%= f.text_field :name  %> 
  </p>  
  
  <p>
    <label>תאריך הפרסום</label><br>
     <%= f_publish_date_select(f)%>
  </p>
  
  <% f.fields_for :document_texts do |fnest| %> 
     <%if fnest.object.language == I18n.locale.to_s %>
         <%= render "form_document_texts", :f => fnest %> 
     <%end%>   
  <% end %>

  
  

